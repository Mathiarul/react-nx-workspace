# order-management

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build order-management` to build the library.

## Running unit tests

Run `nx test order-management` to execute the unit tests via [Vitest](https://vitest.dev/).
